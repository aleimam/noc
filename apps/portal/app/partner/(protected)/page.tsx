// Shared partner dashboard (in @noc/partner-portal; both apps render it inside their own brand
// shell — the (protected) layout).
export { PartnerDashboard as default } from '@noc/partner-portal/server';
export const dynamic = 'force-dynamic';
