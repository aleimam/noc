# dependencies
node_modules/

# builds
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# turborepo
.turbo/

# env files (keep .env.example)
.env
.env.*
!.env.example

# user-generated uploads
/uploads/

# local dev database (portable MariaDB)
/.devdb/

# optional server-side backup credential override (lives only on the server)
ops/backup.env

# logs
*.log
npm-debug.log*
yarn-error.log*

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# business requirement & credential docs (kept local, NEVER published)
*.docx

# local design-system reference + archives (not app code)
/NewObour Design System/
*.zip

# Word lock/temp artifacts
~WRL*.tmp

# source data drops (PII — never commit)
/input data/
